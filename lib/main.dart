import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'screens/editor_screen.dart';
import 'native/bindings.dart';
import 'theme/app_theme.dart';

void main() {
  // Show warning if native library is not available
  if (!isNativeAvailable) {
    print('???????????????????????????????????????????????????????');
    print('??  WARNING: Native library not found!');
    print('???????????????????????????????????????????????????????');
    print('The app is running in DEMO MODE without video processing.');
    print('');
    print('To enable full functionality:');
    print('1. Build the native C++ library using build.bat');
    print('2. Install FFmpeg dependencies');
    print('3. Rebuild and run the app');
    print('???????????????????????????????????????????????????????');
  }

  runApp(const ProviderScope(child: VideoCutApp()));
}

class VideoCutApp extends StatelessWidget {
  const VideoCutApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'FlowCut',
      debugShowCheckedModeBanner: false,
      theme: AppTheme.darkTheme,
      home: const EditorScreen(),
    );
  }
}
